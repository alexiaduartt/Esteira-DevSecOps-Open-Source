## PBI-22: Docker Compose para teste dinâmico (DAST/ZAP)
## Permite subir a aplicação localmente na porta 3000 para validação

services:
  app:
    build: .
    container_name: esteira-devsecops-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
